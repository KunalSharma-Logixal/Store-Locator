services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: storelocator-backend

    ports:
      - "10000:8000"

    volumes:
      - ./data:/app/data

    restart: unless-stopped
